If no configuration file exists, the server creates a new one and starts with a random password.
The random password is shown only once. If you forget it, change it with the following command:
./xxtcloudserver-<platform>-<arch> -set-password <password>

Windows x64 example:
xxtcloudserver-windows-amd64.exe -set-password 12345678
